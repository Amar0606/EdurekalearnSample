pipeline {
    agent any
tools {
  maven 'mvn_home'
}


    stages {
        stage('SCM-Checkout') {
            steps {
                git branch: 'main', credentialsId: 'GitCredentials', url: 'https://github.com/Amar0606/MavenBuild.git'                
            }
        }

        stage('Maven-clean') {
            steps {
               sh'mvn clean'
            
            }
        }
        stage('Maven-Build') {
            steps {
               sh'mvn build'
            
            }
        }

        stage('Step 3 - Package') {
            steps {
                sh'mvn package'
                
            }
        }
    }
}
